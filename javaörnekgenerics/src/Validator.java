public class Validator {
    public<T extends IEntitiy> void validate (T entity ){
        // validator lar bir nesne kurallara uygun mu belirlemek için kullanılırlar 
    // ürün fiyatı 0 dan büyük olmalı gibi 
    


    }
}

// generic yaptık T ile 
// validate(x); x  IEntity türünde olmalı 